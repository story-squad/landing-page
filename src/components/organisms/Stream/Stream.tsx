import React from 'react';
import { StreamText } from '../../molecules';

export default function Stream(): React.ReactElement {
  return (
    <div className="stream-wrapper">
      <StreamText />
    </div>
  );
}
